plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
norm_conju(x2, 5, 12, r=1/0.75^2)
}
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 5, 12, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 4, 125, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 4, 100, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 4, 11, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 4, 7, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 4, 4, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, 4, 2, r=1/0.75^2)
?rexp
?dbeta
?dgmma
?dgamma
n3<-50
x3<-rexp(n3, rate=3)
exp_conju<-function(vec, alpha, beta){
len=length(vec)
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, dgamma(n3, shape=new_alpha, rate = new_beta ))
}
exp_conju(x3, alpha= 3, beta=2)
n3<-50
x3<-rexp(n3, rate=3)
exp_conju<-function(vec, alpha, beta){
len=length(vec)
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, dgamma(x, shape=new_alpha, rate = new_beta ))
}
exp_conju(x3, alpha= 3, beta=2)
n3<-50
x3<-rexp(n3, rate=3)
exp_conju<-function(vec, alpha, beta){
len=length(vec)
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, dgamma(x, shape=new_alpha, rate = new_beta ))
}
exp_conju(x3, alpha= 3, beta=0.2)
n3<-50
x3<-rexp(n3, rate=3)
exp_conju<-function(vec, alpha, beta){
len=length(vec)
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, dgamma(x, shape=new_alpha, rate = new_beta ))
}
exp_conju(x3, alpha= 0.76, beta=0.2)
n3<-50
x3<-rexp(n3, rate=3)
exp_conju<-function(vec, alpha, beta){
len=length(vec)
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, dgamma(x, shape=new_alpha, rate = new_beta ))
}
exp_conju(x3, alpha= 1.76, beta=0.2)
n3<-50
x3<-rexp(n3, rate=3)
exp_conju<-function(vec, alpha, beta){
len=length(vec)
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, dgamma(x, shape=new_alpha, rate = new_beta ))
}
exp_conju(x3, alpha= 0.76, beta=0.2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -3, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -15, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -999, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -100, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -90, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -67, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -20, 2, r=1/0.75^2)
n2<-50
x2<-rnorm(n2, mean = 4, sd=0.75)
norm_conju<-function(vec, mean, precision, r){
len=length(vec)
new_mean=(precision*mean+ len*r*mean(vec))/(precision+n*r)
new_precision=precision+n*r
x<-x <- seq(-10, 10, length=10000)
plot(x, dnorm(x, mean=new_mean, sd=sqrt(1/new_precision)))
}
norm_conju(x2, -11, 2, r=1/0.75^2)
?runif
library(rmutil)
?pow
??pow
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, new_alpha ,new_beta))
}
plot(x, pareto(x, new_alpha ,new_beta))
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
}
plot(x, pareto(x, new_alpha ,new_beta))
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
new_beta=beta+sum(vec)
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, new_alpha ,new_beta))
}
unif_conju(x4, 0.73, 1)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 0.73, 1)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 0.73, -49)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 0.73, -50)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 0.73, -49.5)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 13, -49.5)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 1, -49.5)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 1, -49.5)
n4<-50
x4<-runif(n3, min=0, max=5.7)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 0.004, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 0.004, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 2, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 10, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 20, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 30, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 50, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 100, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, -13, -49.5)
max(vec)
max(x4)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(-10, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 1, -49.5)
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(0, 10, length=10000)
plot(x, pareto(x, omega1, new_alpha))
}
unif_conju(x4, 1, -49.5)
?log
n4<-50
x4<-runif(n3)
pareto<-function(x, eta, theta){
return ( (theta*eta^(theta))/x^(theta+1) )
}
unif_conju<-function(vec, omega0, alpha){
len=length(vec)
omega1<-max(c(omega0, vec))
new_alpha=alpha+len
x<-x <- seq(0, 10, length=10000)
plot(x, log10(pareto(x, omega1, new_alpha) ) )
}
unif_conju(x4, 1, -49.5)
